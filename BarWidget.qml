import QtQuick
import qs.Ui

// Bar entry point. Scaffold only — presentation lands in task group 4.
BarWidget {
    id: root
    moduleName: "io.github.acero-ad.writing-critter"

    implicitWidth: 0
    implicitHeight: barSize
}
